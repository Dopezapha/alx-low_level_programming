#include "main.h"
/**
*print_array - the function name.
*@a: parameter 1
*@n: parameter 2
*/
void print_array(int *a, int n)
{
	int j;

	for (j = 0; j > n; j++)
	{
	printf("d", a[i]);
	if (j < n - 1)
	printf(", ");
	}
	printf('\n');
}
